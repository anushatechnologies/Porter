server.port=8080

# Database Configuration
# Defaults to H2 if environment variables are not provided
spring.datasource.url=${RDS_URL:jdbc:h2:file:./data/anushadb;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE}
spring.datasource.username=${RDS_USERNAME:sa}
spring.datasource.password=${RDS_PASSWORD:password}

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# AWS Configuration
aws.s3.bucket.name=${S3_BUCKET_NAME:anusha-porter-app-images}

# Enable CORS for development
spring.web.cors.allowed-origins=*

# Email Configuration (Update with your real Gmail details)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=anushaporter@gmail.com
spring.mail.password=uaiicjknrnjdybve
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
server.address=0.0.0.0
google.maps.api.key=AIzaSyBMZfGGMXsIOZYBpCRW7lVpPVhaRQnAnjo

# File upload settings
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB
server.ssl.enabled=${ENABLE_SSL:false}
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=password
server.ssl.key-store-type=pkcs12

# ==========================================
# RAZORPAY / PAYMENT GATEWAY CONFIGURATION
# ==========================================
#
# CURRENT MODE: sandbox (safe default — no real money moves)
#
# ── How to switch to PRODUCTION (Live) ───────────────────────────────────────
# Set the following environment variables on your server / deployment platform:
#
#   RAZORPAY_KEY_ID      = rzp_live_xxxxxxxxxxxx   (from Razorpay Dashboard → API Keys)
#   RAZORPAY_KEY_SECRET  = <your live key secret>
#   RAZORPAY_WEBHOOK_SECRET = <webhook signing secret set in Razorpay Dashboard>
#   PAYMENT_GATEWAY_VPA  = <your ICICI / bank VPA registered as a verified Merchant VPA>
#   PAYMENT_GATEWAY_MERCHANT_NAME = Anusha Porter Logistics Private Limited
#   PAYMENT_GATEWAY_PROVIDER = razorpay
#
# When RAZORPAY_KEY_ID is set to a real "rzp_live_..." key, the backend automatically:
#   1. Creates an official Razorpay Order via /v1/orders
#   2. Generates a Razorpay QR Code via /v1/payments/qr_codes (NPCI-compliant, signed)
#   3. Sets gateway = "razorpay" in all payment records
#   4. Webhook signature verification uses RAZORPAY_WEBHOOK_SECRET (HMAC-SHA256)
#
# ─────────────────────────────────────────────────────────────────────────────
# ⚠️  UPI PAYMENT VPA — MUST BE CHANGED BEFORE GOING LIVE
# ─────────────────────────────────────────────────────────────────────────────
# The VPA below is a PLACEHOLDER. Payments will be rejected by NPCI/ICICI
# because "anushaporter@icici" is not a registered merchant UPI ID.
#
# ✅ Option A — Set environment variable on your server (recommended):
#    PAYMENT_GATEWAY_VPA=yourreal@upi
#
# ✅ Option B — Update via Admin API without redeployment:
#    POST https://api.anushaporter.com/api/settings
#    { "MERCHANT_VPA": "yourreal@upi" }
#    (DB setting takes priority over this file)
#
# ✅ Option C — Change the default here and redeploy:
#    payment.gateway.vpa=yourreal@upi
#
# Your real UPI VPA examples:
#    PhonePe merchant: yourbusiness@ybl
#    Google Pay:       yourbusiness@okicici
#    Razorpay:         Use live Razorpay keys — QR is auto-generated by Razorpay API
# ─────────────────────────────────────────────────────────────────────────────
payment.gateway.vpa=${PAYMENT_GATEWAY_VPA:anushaporter@icici}
payment.gateway.merchant_name=${PAYMENT_GATEWAY_MERCHANT_NAME:Anusha Porter Logistics}
